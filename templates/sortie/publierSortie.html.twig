{% extends 'base.html.twig' %}
{% block body %}

    <main class="container-fluid">
        <div>
            {{ form_start(sortieForm) }}
            {{ form_widget(sortieForm) }}
            <button name="publier" value="publier" class="btn btn-primary">Publier une sortie</button>
            <a href="{{ path('sortie_accueil') }}" class="btn btn-primary">Retour</a>
            {{ form_end(sortieForm) }}
        </div>
    </main>

{% endblock %}


{% block title %}
    {{ parent() }} | Publier une sortie
{% endblock %}



