{% extends 'base.html.twig' %}
{% block body %}

    <main class="container-fluid">
        <div>
            {{ form_start(sortieForm) }}
            {{ form_widget(sortieForm) }}
            <button name="publier" value="publier">Publier une sortie</button>
            <button><a href="{{ path('sortie_accueil') }}">Retour</a></button>
            {{ form_end(sortieForm) }}
            <a type="btn btn-primary" {{ path('sortie_accueil') }}>Retour</a>
        </div>
    </main>

{% endblock %}


{% block title %}
    {{ parent() }} | Publier une sortie
{% endblock %}



