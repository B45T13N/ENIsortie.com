<nav>
    <ul>
        {% if is_granted("ROLE_ADMIN") %}
        <li>
            <a href="#">Villes</a>
        </li>
        <li>
            <a href="#">Campus</a>
        </li>
        {% endif %}
        {% if app.user %}
        <li>
            <a href="#">Accueil</a>
        </li>
        <li>
            <a href="#">Mon profil</a>
        </li>

        <li>
            <a href="{{ path('app_logout') }}">se déconnecter</a>
        </li>
        {% endif %}
        {% if not app.user %}
        <li>
            <a href="{{ path('app_login') }}">se connecter</a>
        </li>
        {% endif %}
    </ul>

</nav>

