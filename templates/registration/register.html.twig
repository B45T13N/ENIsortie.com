{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Enregistrer un utilisateur{% endblock %}

{% block body %}
    <main>
        <h1>Enregistrer</h1>

        {{ form_start(registrationForm) }}
           {{ form_widget(registrationForm) }}
            <button>Enregistrer</button> <button>Import CSV</button>
        {{ form_end(registrationForm) }}
    </main>
{% endblock %}
